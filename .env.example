VERSION=0.5.4

# Nest run in docker, change host to database container name
DB_HOST=localhost
DB_PORT=5432

# POSTGRES
POSTGRES_USER=docker
POSTGRES_PASSWORD=docker
POSTGRES_DB=core

LOGIN_ORIGIN=http://localhost:3033
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?schema=core

# This sets the URL used for direct connections to the database and should only be needed in limited circumstances
# See: https://www.prisma.io/docs/reference/api-reference/prisma-schema-reference#fields:~:text=the%20shadow%20database.-,directUrl,-No
DIRECT_URL=${DATABASE_URL}

REMIX_APP_PORT=3033
APP_ENV=production
NODE_ENV=${APP_ENV}
APP_ORIGIN=http://localhost:3033
API_BASE_URL=${APP_ORIGIN}


SESSION_SECRET=27192e6432564f4788d55c15131bd5ac
ENCRYPTION_KEY=27192e6432564f4788d55c15131bd5ac

########### Sign.in with google ############
AUTH_GOOGLE_CLIENT_ID=
AUTH_GOOGLE_CLIENT_SECRET=

REDIS_HOST=redis
REDIS_PORT=6379
REDIS_TLS_DISABLED=true

ENABLE_EMAIL_LOGIN=true

NEO4J_URI=bolt://neo4j:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=27192e6432564f4788d55c15131bd5ac

########### LLM API Keys ############
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_GENERATIVE_AI_API_KEY=

# OpenRouter - Access 300+ models with a single API key
# Get your key at https://openrouter.ai/keys
OPENROUTER_API_KEY=

MAGIC_LINK_SECRET=27192e6432564f4788d55c15131bd5ac
NEO4J_AUTH=neo4j/27192e6432564f4788d55c15131bd5ac
OLLAMA_URL=http://ollama:11434

########### Model Configuration ############
# Chat model - use direct API format or OpenRouter format
# Direct API examples: gpt-4.1-2025-04-14, claude-sonnet-4-20250514, gemini-2.5-pro-preview-03-25
# OpenRouter examples: deepseek/deepseek-chat-v3, meta-llama/llama-3.3-70b-instruct, anthropic/claude-sonnet-4
MODEL=gpt-4.1-2025-04-14

# Embedding model - supports local (free) or cloud options
# Local models (free, no API needed):
#   all-minilm (384d), nomic-embed-text (768d), bge-small (384d), gte-small (384d)
# Cloud models:
#   Direct: text-embedding-3-small (requires OPENAI_API_KEY)
#   OpenRouter: openai/text-embedding-3-small (requires OPENROUTER_API_KEY)
EMBEDDING_MODEL=all-minilm
EMBEDDING_MODEL_SIZE=384

# Local embedding cache directory (for local models)
LOCAL_EMBEDDING_CACHE_DIR=./.cache/transformers

########### Reranking Configuration ############
# Choose reranking provider: 'cohere', 'ollama', or 'none'
# - 'cohere': Use Cohere API for high-quality reranking (requires COHERE_API_KEY)
# - 'ollama': Use local Ollama model for reranking (requires OLLAMA_URL and a rerank model)
# - 'none': No reranking, use original search algorithm scores
RERANK_PROVIDER=none

# Cohere settings (when RERANK_PROVIDER=cohere)
COHERE_API_KEY=
COHERE_RERANK_MODEL=rerank-english-v3.0
COHERE_SCORE_THRESHOLD=0.3

# Ollama settings (when RERANK_PROVIDER=ollama)
# Model examples: qwen2.5:14b-rerank, bge-reranker-v2-m3
OLLAMA_RERANK_MODEL=dengcao/Qwen3-Reranker-8B:Q5_K_M
OLLAMA_SCORE_THRESHOLD=0.3

## AWS Bedrock ##
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

QUEUE_PROVIDER=bullmq

# WhatsApp (Twilio)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=

# Email (Resend)
EMAIL_TRANSPORT=resend
RESEND_API_KEY=
FROM_EMAIL=
